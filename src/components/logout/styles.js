import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    logoutImage:{
        width: 25,
        height: 25,
        resizeMode: "cover",
        justifyContent: "flex-end",
        marginRight: 15
    }
})

export {styles}